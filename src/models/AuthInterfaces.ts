export interface LocationState {
  accessToken: string;
}

export interface UserData {
  firstname: string;
  lastname: string;
  username: string;
  userId: number;
}

export interface LoginResponse {
  access_token: string;
}
