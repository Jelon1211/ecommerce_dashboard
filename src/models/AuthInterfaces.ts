export interface LocationState {
  accessToken: string;
  username: string;
}

export interface UserData {
  firstname: string;
  lastname: string;
  username: string;
  userId: number;
}

export interface LoginResponse {
  accessToken: string;
}
